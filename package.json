{
  "name": "KnightModelsApp",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "meteor-node-stubs": "^0.4.1",
    "node-pre-gyp": "^0.12.0",
    "update": "^0.7.4"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
